import { Card } from "@/components/ui/Card";

export function AdminDetailPanel() {
  return (
    <Card title="Detail Panel">
      <p className="text-sm text-[var(--color-muted)]">Controlled placeholder for order, supplier, settlement, or risk details.</p>
    </Card>
  );
}
